after adding storageClassName: aws-gp2 to volumeClaimTemplates, everything resolved

      volumeClaimTemplates:
      - metadata:
          namespace: staging
          name: data
          labels:
            name: redis-cluster
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: aws-gp2
          resources:
            requests:
              storage: 100Mi

storageClassName: "my-storage-class"

helm install my-chart --set storageClassName=other-storage-class


waiting for a volume to be created, either by external provisioner "ebs.csi.aws.com" or manually created by system administrator
https://stackoverflow.com/questions/69290796/persistent-storage-in-eks-failing-to-provision-volume

and

waiting for first consumer to be created before binding